name: test
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
       # Set fetch-depth: 0 to fetch commit history and tags for use in version calculation
      - name: Check out code
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
          submodules: recursive
      
      - name: create checksum file
        uses: hypertrace/github-actions/checksum@main

      - name: Cache packages
        id: cache-packages
        uses: actions/cache@v2
        with:
          path: ~/.gradle
          key: gradle-packages-${{ runner.os }}-${{ github.job }}-${{ hashFiles('**/checksum.txt') }}
          restore-keys: |
            gradle-packages-${{ runner.os }}-${{ github.job }}
            gradle-packages-${{ runner.os }}

      - name: list files 1
        run: ls -lrt javaagent-core/src/main/proto/tools/env-vars-generator/protobuf/third_party/benchmark/datasets

      - name: list files 2
        run: ls -lrt javaagent-core/src/main/proto/tools/env-vars-generator/protobuf/third_party/benchmark/datasets/google_message3

      - name: build
        run: make build
        env: 
          JVM_OPTS: -Xmx1g
          TERM: dumb

  smoke-test:
    runs-on: ubuntu-20.04
    steps:
       # Set fetch-depth: 0 to fetch commit history and tags for use in version calculation
      - name: Check out code
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
          submodules: recursive
      
      - name: create checksum file
        uses: hypertrace/github-actions/checksum@main

      - name: Cache packages
        id: cache-packages
        uses: actions/cache@v2
        with:
          path: ~/.gradle
          key: gradle-packages-${{ runner.os }}-${{ github.job }}-${{ hashFiles('**/checksum.txt') }}
          restore-keys: |
            gradle-packages-${{ runner.os }}-${{ github.job }}
            gradle-packages-${{ runner.os }}

      #- name: init submodules
      #  run: make init-submodules

      - name: list files 1
        run: ls -lrt javaagent-core/src/main/proto/tools/env-vars-generator/protobuf/third_party/benchmark/datasets

      - name: list files 2
        run: ls -lrt javaagent-core/src/main/proto/tools/env-vars-generator/protobuf/third_party/benchmark/datasets/google_message3

      - name: smoke-test
        run: make smoke-test
        env: 
          JVM_OPTS: -Xmx1g
          TERM: dumb

  muzzle:
    runs-on: ubuntu-20.04
    steps:
       # Set fetch-depth: 0 to fetch commit history and tags for use in version calculation
      - name: Check out code
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
      
      - name: create checksum file
        uses: hypertrace/github-actions/checksum@main

      - name: Cache packages
        id: cache-packages
        uses: actions/cache@v2
        with:
          path: ~/.gradle
          key: gradle-packages-${{ runner.os }}-${{ github.job }}-${{ hashFiles('**/checksum.txt') }}
          restore-keys: |
            gradle-packages-${{ runner.os }}-${{ github.job }}
            gradle-packages-${{ runner.os }}

      - name: muzzle
        run: make muzzle
        env: 
          JVM_OPTS: -Xmx1g
          TERM: dumb